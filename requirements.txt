# NeoMarket — зависимости проекта
# Установка: python -m pip install -r requirements.txt
#
# Прямые зависимости (используются в коде):
#   fastapi, starlette, uvicorn — HTTP API
#   pydantic, pydantic-settings, email-validator — схемы (EmailStr в auth)
#   SQLAlchemy, asyncpg, greenlet — PostgreSQL (async)
#   httpx — исходящие HTTP-запросы (B2B / Moderation / B2C)
#   python-jose, cryptography — JWT (auth, checkout)
#   python-dotenv — загрузка .env в Settings
#
# Тесты:
#   pytest, pytest-asyncio
#   httpx — AsyncClient / ASGITransport в тестах
#
# Ниже — полный pin транзитивных пакетов для воспроизводимых сборок.

# --- Runtime: web ---
fastapi==0.115.0
starlette==0.38.6
uvicorn==0.30.0
httptools==0.7.1
watchfiles==1.1.1
websockets==16.0

# --- Runtime: validation & config ---
pydantic==2.13.4
pydantic-settings==2.14.1
pydantic_core==2.46.4
annotated-types==0.7.0
typing_extensions==4.15.0
typing-inspection==0.4.2
email-validator==2.3.0
dnspython==2.8.0
python-dotenv==1.0.1

# --- Runtime: database ---
SQLAlchemy==2.0.49
asyncpg==0.31.0
greenlet==3.5.0

# --- Runtime: HTTP client ---
httpx==0.27.0
httpcore==1.0.9
h11==0.16.0
anyio==4.13.0
sniffio==1.3.1
certifi==2026.4.22
idna==3.14

# --- Runtime: auth (JWT) ---
python-jose==3.5.0
cryptography==48.0.0
ecdsa==0.19.2
rsa==4.9.1
pyasn1==0.6.3
cffi==2.0.0
pycparser==3.0
six==1.17.0

# --- Runtime: FastAPI / Starlette extras ---
annotated-doc==0.0.4
click==8.3.3
colorama==0.4.6

# --- Dev & test ---
pytest==8.3.0
pytest-asyncio==1.3.0
iniconfig==2.3.0
packaging==26.2
pluggy==1.6.0
